import React, {Component} from "react";
import withStyles from "@material-ui/core/styles/withStyles";

const styles = ({});

class UpdateStudent extends Component {
    render() {
        return (
            <h1>
                Update Student Page
            </h1>
        );
    }
}

export default withStyles(styles)(UpdateStudent);
